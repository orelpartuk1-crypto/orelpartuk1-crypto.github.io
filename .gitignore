node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Netlify deploy token (never commit)
.netlify-token

# GitHub deploy token, scoped only to this repo (never commit)
.github-token

# Local Netlify folder
.netlify

# Local Supabase CLI cache (project ref, pooler host, tool versions)
supabase/.temp
